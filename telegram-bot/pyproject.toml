[project]
name = "telegram-bot"
version = "0.1.0"
description = "Telegram bot for reporting lost dogs"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot[webhooks]>=21.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
